package horseracing;

public class Betting {
 /*public int odds(Horse horse) {

        int c = Race.class(int)(7 - Math.abs(Race.horse.getPreferredLength() - Race.raceLength));

        if (Race.class.raceSurface.equalsIgnoreCase("grass"))
            c += horse.getGrassRating() / 2;
        else if (raceSurface.equalsIgnoreCase("dirt"))
            c += horse.getDirtRating() / 2;
        else if (raceSurface.equalsIgnoreCase("mud"))
            c += horse.getMudRating() / 2;
       
    };
*/

}
//Math.random()*(7-preferred-length)
